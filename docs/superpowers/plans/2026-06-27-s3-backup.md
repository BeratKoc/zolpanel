# Offsite / S3 Backup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Mevcut yerel yedekleri (BACKUP_DIR'deki tar) S3-uyumlu uzak depoya (AWS/R2/MinIO/Wasabi) gönder; uzak yedekleri listele; bağlantıyı test et. S3 kimlik bilgileri şifreli saklanır.

**Architecture:** El-yapımı AWS **SigV4** imzalayıcı (saf node:crypto — yeni bağımlılık yok, RFC test vektörüyle test edilir) + minimal S3 client (fetch ile PutObject/ListObjectsV2); şifreli S3-config (Task-DNS'teki secrets+settings-kv); Next route'ları; backups sayfasına S3 entegrasyonu.

**Tech Stack:** Next.js 15 route handlers (nodejs), node:crypto (SigV4), fetch (S3 HTTP API), TypeScript, node:test, next-intl (6 dil), Playwright.

## Global Constraints
- Yeni heavy-dep YOK — SigV4 el-yapımı (saf). S3 secret key **AES-GCM şifreli** (mevcut `encryptSecret`) settings-kv'de (`s3_config` anahtarı, JSON; secretKey alanı şifreli).
- Yedek dosya adı `assertSafeBackupName` ile doğrulanır (mevcut). Hepsi `requireAuth`. Commit yazarı `BeratKoc <ahmetberatkoc0@gmail.com>`; **Co-Authored-By YOK**.
- Test `npm test`; build `npm run build` (Win EPERM→`rm -rf .next`); `npx tsc --noEmit` temiz; 6-dil i18n parity.
- Mevcut: `requireAuth`/`unauthorized`; `encryptSecret`/`decryptSecret` (`@/lib/server/secrets`); `getSetting`/`setSetting`/`deleteSetting` (`@/lib/server/db`); `BACKUP_DIR`/`listBackups`/`backupFilePath`/`assertSafeBackupName` (`@/lib/server/backup`); `api` `request`.

## Dosya yapısı
- `lib/server/s3/sigv4.ts` — `sha256hex`/`getSignatureKey`/`signS3Request` (saf). 
- `lib/server/s3/sigv4.test.ts` — unit testler (AWS test vektörü).
- `lib/server/s3/client.ts` — `S3Config` tipi + `putObject`/`listObjects`/`testConnection` (fetch + sigv4).
- `lib/server/s3/config.ts` — `getS3Config`/`saveS3Config`/`deleteS3Config` (settings-kv + encrypt secretKey) + `validateS3Config`.
- `lib/server/s3/config.test.ts` — `validateS3Config` testleri.
- `app/api/s3/config/route.ts` (GET status/POST/DELETE), `app/api/s3/test/route.ts` (POST), `app/api/s3/upload/route.ts` (POST {name}), `app/api/s3/list/route.ts` (GET).
- `app/(panel)/backups/page.tsx` — S3 config kartı + per-backup "S3'e gönder" + uzak liste (mevcut sayfaya ekle).
- nav yok (backups altında); `lib/api-client.ts`; `messages/{6}.json`.

---

### Task 1: SigV4 imzalayıcı + S3 config doğrulayıcı + testler

**Files:** Create `lib/server/s3/sigv4.ts`, `lib/server/s3/sigv4.test.ts`, `lib/server/s3/config.ts` (validateS3Config + types only this task; storage funcs Task 2), `lib/server/s3/config.test.ts`.

**Interfaces:** Produces `sha256hex(data:string|Buffer):string`; `getSignatureKey(secret:string,dateStamp:string,region:string,service:string):Buffer`; `signS3Request(opts):{ headers:Record<string,string>; url:string }`; `interface S3Config { endpoint:string; region:string; bucket:string; accessKeyId:string; secretAccessKey:string; prefix?:string }`; `validateS3Config(c:Partial<S3Config>):string|null`.

- [ ] **Step 1: `lib/server/s3/sigv4.ts`:**
```ts
import crypto from 'node:crypto';

function hmac(key: crypto.BinaryLike, data: string): Buffer {
  return crypto.createHmac('sha256', key).update(data, 'utf8').digest();
}
export function sha256hex(data: string | Buffer): string {
  return crypto.createHash('sha256').update(data).digest('hex');
}
/** AWS SigV4 imzalama anahtarı (HMAC zinciri). */
export function getSignatureKey(secret: string, dateStamp: string, region: string, service: string): Buffer {
  const kDate = hmac('AWS4' + secret, dateStamp);
  const kRegion = hmac(kDate, region);
  const kService = hmac(kRegion, service);
  return hmac(kService, 'aws4_request');
}

export interface SignOpts {
  method: string; endpoint: string; region: string; bucket: string;
  key: string; accessKeyId: string; secretAccessKey: string;
  payload: Buffer; amzDate: string; dateStamp: string; // amzDate=YYYYMMDDTHHMMSSZ, dateStamp=YYYYMMDD
  query?: string; extraHeaders?: Record<string, string>;
}
/** S3 path-style isteği imzalar; {url, headers} döner. */
export function signS3Request(o: SignOpts): { url: string; headers: Record<string, string> } {
  const service = 's3';
  const host = o.endpoint.replace(/^https?:\/\//, '').replace(/\/$/, '');
  const canonicalUri = `/${o.bucket}/${o.key}`.replace(/\/+/g, '/');
  const canonicalQuery = o.query || '';
  const payloadHash = sha256hex(o.payload);
  const headers: Record<string, string> = {
    host,
    'x-amz-content-sha256': payloadHash,
    'x-amz-date': o.amzDate,
    ...(o.extraHeaders || {}),
  };
  const sortedHeaderKeys = Object.keys(headers).map(k => k.toLowerCase()).sort();
  const canonicalHeaders = sortedHeaderKeys.map(k => `${k}:${headers[Object.keys(headers).find(h => h.toLowerCase() === k)!].trim()}\n`).join('');
  const signedHeaders = sortedHeaderKeys.join(';');
  const canonicalRequest = [o.method, canonicalUri, canonicalQuery, canonicalHeaders, signedHeaders, payloadHash].join('\n');
  const scope = `${o.dateStamp}/${o.region}/${service}/aws4_request`;
  const stringToSign = ['AWS4-HMAC-SHA256', o.amzDate, scope, sha256hex(canonicalRequest)].join('\n');
  const signingKey = getSignatureKey(o.secretAccessKey, o.dateStamp, o.region, service);
  const signature = crypto.createHmac('sha256', signingKey).update(stringToSign, 'utf8').digest('hex');
  headers['Authorization'] = `AWS4-HMAC-SHA256 Credential=${o.accessKeyId}/${scope}, SignedHeaders=${signedHeaders}, Signature=${signature}`;
  return { url: `https://${host}${canonicalUri}${canonicalQuery ? '?' + canonicalQuery : ''}`, headers };
}
```

- [ ] **Step 2: `lib/server/s3/config.ts` (yalnız validate + tip bu task):**
```ts
export interface S3Config { endpoint: string; region: string; bucket: string; accessKeyId: string; secretAccessKey: string; prefix?: string; }
export function validateS3Config(c: Partial<S3Config>): string | null {
  if (!c.endpoint || !/^https?:\/\/.+/.test(c.endpoint)) return 'Geçerli endpoint (https://...) gerekli';
  if (!c.region || !c.region.trim()) return 'Region gerekli';
  if (!c.bucket || !/^[a-z0-9.\-]{3,63}$/.test(c.bucket)) return 'Geçerli bucket adı gerekli';
  if (!c.accessKeyId || !c.accessKeyId.trim()) return 'Access Key ID gerekli';
  if (!c.secretAccessKey || !c.secretAccessKey.trim()) return 'Secret Access Key gerekli';
  return null;
}
```

- [ ] **Step 3: testler** — `lib/server/s3/sigv4.test.ts` (AWS dökümante test vektörü — signing key):
```ts
import { test } from 'node:test';
import assert from 'node:assert';
import { getSignatureKey, sha256hex } from './sigv4';

test('getSignatureKey: AWS dökümante test vektörü', () => {
  // AWS docs "Deriving the signing key" örneği
  const key = getSignatureKey('wJalrXUtnFEMI/K7MDENG+bPxRfiCYEXAMPLEKEY', '20120215', 'us-east-1', 'iam');
  assert.strictEqual(key.toString('hex'), 'f4780e2d9f65fa895f9c67b32ce1baf0b0d8a43505a000a1a9e090d414db404d');
});
test('sha256hex bilinen değer', () => {
  assert.strictEqual(sha256hex(''), 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
});
```
`lib/server/s3/config.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert';
import { validateS3Config } from './config';
test('validateS3Config', () => {
  assert.strictEqual(validateS3Config({ endpoint:'https://s3.amazonaws.com', region:'us-east-1', bucket:'my-bucket', accessKeyId:'AK', secretAccessKey:'SK' }), null);
  assert.strictEqual(validateS3Config({ endpoint:'ftp://x', region:'r', bucket:'b1234', accessKeyId:'a', secretAccessKey:'s' }), 'Geçerli endpoint (https://...) gerekli');
  assert.strictEqual(validateS3Config({ endpoint:'https://x', region:'us', bucket:'AB', accessKeyId:'a', secretAccessKey:'s' }), 'Geçerli bucket adı gerekli');
  assert.match(validateS3Config({ endpoint:'https://x', region:'us', bucket:'okbucket', accessKeyId:'', secretAccessKey:'s' }) || '', /Access Key/);
});
```

- [ ] **Step 4:** `npm test` FAIL→PASS (sigv4 test vektörü tutmalı); `npx tsc --noEmit` temiz.
- [ ] **Step 5: Commit** `git add lib/server/s3/sigv4.ts lib/server/s3/sigv4.test.ts lib/server/s3/config.ts lib/server/s3/config.test.ts && git commit -m "feat(s3): sigv4 signer + s3 config validator + tests (AWS vector)"`

---

### Task 2: S3 client + config storage + route'lar + api-client

**Files:** Create `lib/server/s3/client.ts`, append storage funcs to `lib/server/s3/config.ts`, `app/api/s3/config/route.ts`, `app/api/s3/test/route.ts`, `app/api/s3/upload/route.ts`, `app/api/s3/list/route.ts`. Modify `lib/api-client.ts`.

**Interfaces:** Consumes Task 1 (`signS3Request`, `validateS3Config`, `S3Config`), secrets, settings-kv, backup helpers. Produces: config GET(status, secret HARİÇ)/POST/DELETE; test POST; upload POST `{name}` (yereldeki yedeği S3'e PUT); list GET (uzak yedekler). api-client `s3ConfigStatus/s3ConfigSave/s3ConfigDelete/s3Test/s3Upload/s3List`.

- [ ] **Step 1: config storage** (`config.ts`'e ekle): `saveS3Config(c)` → secretAccessKey'i `encryptSecret`'le, JSON'u `setSetting('s3_config', json)`; `getS3Config(): S3Config|null` → parse + secretAccessKey'i `decryptSecret`; `deleteS3Config()` → `deleteSetting`. `getS3ConfigSafe()` → secretAccessKey'siz (status için).

- [ ] **Step 2: `lib/server/s3/client.ts`** — `now()` → `{amzDate, dateStamp}`; `putObject(cfg, key, body:Buffer, contentType)` → `signS3Request({method:'PUT',...})` → `fetch(url,{method:'PUT',headers,body})`; non-2xx → throw (gövde mesajı). `listObjects(cfg)` → GET `?list-type=2&prefix=...` (query imzalı) → XML parse (basit regex `<Key>...</Key>` + `<Size>`). `testConnection(cfg)` → listObjects (1 obje) çağır, hata yoksa ok. Key = `(cfg.prefix||'') + name`.

- [ ] **Step 3: route'lar** (hepsi requireAuth):
  - `config/route.ts`: GET → `{ configured: !!getSetting('s3_config'), config: getS3ConfigSafe() }` (secret YOK); POST `{config}` → `validateS3Config` (400) → `saveS3Config`; DELETE → `deleteS3Config`.
  - `test/route.ts`: POST → `getS3Config()` (yoksa 400) → `testConnection` → `{ok:true}` veya hata.
  - `upload/route.ts`: POST `{name}` → `assertSafeBackupName(name)` → dosyayı `backupFilePath(name)`'ten oku (`fs.readFile`) → `getS3Config()` → `putObject(cfg, name, buf, 'application/gzip')` → `{ok:true}`.
  - `list/route.ts`: GET → `getS3Config()` → `{ objects: await listObjects(cfg) }`.
- [ ] **Step 4: api-client:**
```ts
  s3ConfigStatus: () => request('GET', '/s3/config'),
  s3ConfigSave: (config: unknown) => request('POST', '/s3/config', { config }),
  s3ConfigDelete: () => request('DELETE', '/s3/config'),
  s3Test: () => request('POST', '/s3/test'),
  s3Upload: (name: string) => request('POST', '/s3/upload', { name }),
  s3List: () => request('GET', '/s3/list'),
```
- [ ] **Step 5:** `npx tsc --noEmit` + `npm run build` + `npm test` PASS.
- [ ] **Step 6: Commit** `git add lib/server/s3/client.ts lib/server/s3/config.ts "app/api/s3" lib/api-client.ts && git commit -m "feat(s3): s3 client + config storage + routes (status/test/upload/list)"`

---

### Task 3: Backups sayfasına S3 entegrasyonu + i18n

**Files:** Modify `app/(panel)/backups/page.tsx`, `lib/api-client.ts` (zaten), `messages/{6}.json`.

- [ ] **Step 1: i18n (6 dil)** — `backups` bloğuna S3 anahtarları ekle (veya yeni `s3` bloğu): `s3Title, s3Configure, s3Endpoint, s3Region, s3Bucket, s3AccessKey, s3SecretKey, s3Prefix, s3Save, s3Test, s3TestOk, s3Delete, s3Push, s3Pushed, s3Remote, s3NotConfigured, s3Configured`. Tüm 6 dilde parity.
- [ ] **Step 2: backups sayfasına ekle** — Mevcut yedek listesinin üstüne/yanına **S3 bölümü**: `api.s3ConfigStatus()` → configured değilse "S3 yapılandır" kartı (endpoint/region/bucket/accessKey/secretKey(password)/prefix inputları → client `validateS3Config` → `s3ConfigSave`); configured ise: özet (endpoint/bucket) + "Test" (`s3Test`→toast) + "Yapılandırmayı sil" + uzak liste (`s3List`→tablo). Her yerel yedek satırına **"S3'e gönder"** butonu (`s3Upload(name)`→toast). `validateS3Config`'i `@/lib/server/s3/config`'ten import (saf — node import yok → güvenli; emin olunmazsa inline). `useToast`. Mobil uyumlu.
- [ ] **Step 3:** `npx tsc --noEmit` + `npm run build` + `npm test` (i18n parity) PASS.
- [ ] **Step 4: Commit** `git add "app/(panel)/backups/page.tsx" messages/tr.json messages/en.json messages/zh.json messages/es.json messages/de.json messages/fr.json && git commit -m "feat(s3): backups page S3 config + push + remote list + i18n"`

---

### Task 4: e2e + deploy

**Files:** Modify `e2e/backups.spec.ts` (veya yeni `e2e/s3.spec.ts`).

- [ ] **Step 1: e2e** — login → Yedekler → S3 bölümü görünür → configured değilken "S3 yapılandır" kartı/inputları görünür (CI'da config yok) → sayfa çökmez → 360px taşma yok. (NOT: mevcut `backups.spec.ts` Windows-stale-server sorunlu; yeni `e2e/s3.spec.ts` yaz ki izole çalışsın.)
- [ ] **Step 2:** `npx tsc --noEmit`; `npm test`; `npm run e2e` (s3 + mevcut; backups stale→`rm -rf .next`+tekrar). `git push` → CI yeşil.
- [ ] **Step 3: Deploy** `bash deploy.sh` → health + caddy Valid.
- [ ] **Step 4: Canlı doğrulama** — `/backups` S3 bölümü 200; config yoksa kart; `/api/s3/config` no-auth→401. (Token'lı uçtan-uca: kullanıcı S3 kimlik bilgisi girince.)
- [ ] **Step 5: Ledger + tamam.**

## Self-Review (yazar)
- Kapsam: sigv4+validate→T1; client+config-storage+route'lar→T2; UI→T3; e2e+deploy→T4. Güvenlik: secretKey AES-GCM şifreli + asla dönmez, requireAuth her route, backup adı assertSafeBackupName, sigv4 doğru imza. Test-edilebilir saf çekirdek: sigv4 (AWS vektör) + validate. Tip tutarlılığı: `S3Config`/`signS3Request`/`validateS3Config`/`getS3Config` T1↔T2↔T3. Yeni heavy-dep yok. Placeholder yok.
