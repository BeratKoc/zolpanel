# Firewall (UFW) UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Panelden UFW firewall'ı yönet — durum gör, kural ekle/sil (port/proto/IP), etkinleştir/kapat — **admin'i SSH'tan/panelden kilitlemeden** (kritik: 22/80/443 her zaman korunur).

**Architecture:** Saf, test-edilebilir `ufw status` parser + kural-arg builder + giriş doğrulayıcı çekirdeği; Next API route'ları (`ufw ...` `execFile`) onu sarar + SSH-safety guard'ı; frontend firewall sayfası.

**Tech Stack:** Next.js 15 route handlers (nodejs), `child_process.execFile` (`ufw`), TypeScript, node:test, next-intl (6 dil), Playwright.

## Global Constraints
- **SSH-LOCKOUT KORUMASI (kritik):** `ufw enable` ÇAĞRILMADAN ÖNCE 22/tcp + 80/tcp + 443/tcp allow kuralları garanti edilir (yoksa eklenir). 22/tcp allow kuralının silinmesi **engellenir** (400 + uyarı). Bugünkü iptables-kilit olayının dersi.
- Kural girişi doğrulanır: port 1-65535, proto ∈ {tcp,udp,any}, from IP basit IPv4/IPv6/`any` regex. ufw arg'ları **dizi** (shell yok).
- Hepsi `requireAuth`. Commit yazarı `BeratKoc <ahmetberatkoc0@gmail.com>`; **Co-Authored-By YOK**.
- Test `npm test`; build `npm run build` (Win EPERM→`rm -rf .next`); `npx tsc --noEmit` temiz; 6-dil i18n parity.
- Mevcut: `requireAuth`/`unauthorized`; `api` `request`; nav + lucide. ufw sunucuda kurulu (şu an inactive).
- **Canlı doğrulama SADECE okuma** (status) — production'da ufw enable/değişiklik kilitlenme riski; gerçek enable kullanıcının bilinçli aksiyonu.

## Dosya yapısı
- `lib/server/firewall/ufw.ts` — `parseUfwStatus`/`buildUfwAddArgs`/`validateRule`/`isProtectedPort` + tipler.
- `lib/server/firewall/ufw.test.ts` — unit testler.
- `lib/server/firewall/exec.ts` — `ufwStatus()`/`ufwAdd(rule)`/`ufwDeleteByNum(n)`/`ufwEnable()`/`ufwDisable()` (execFile, enable öncesi SSH-safety).
- `app/api/firewall/route.ts` (GET status, POST add), `app/api/firewall/[num]/route.ts` (DELETE), `app/api/firewall/toggle/route.ts` (POST enable/disable).
- `app/(panel)/firewall/page.tsx`; nav; `lib/api-client.ts`; `messages/{6}.json`.

---

### Task 1: Saf ufw parser + arg builder + validator + testler

**Files:** Create `lib/server/firewall/ufw.ts`, `lib/server/firewall/ufw.test.ts`.

**Interfaces:** Produces `interface UfwRule { num:number; to:string; action:string; from:string }`; `interface UfwStatus { active:boolean; rules:UfwRule[] }`; `interface RuleInput { action:'allow'|'deny'; port:number; proto:'tcp'|'udp'|'any'; from?:string }`; `function parseUfwStatus(output:string): UfwStatus`; `function validateRule(r:RuleInput): string|null` (hata mesajı veya null); `function buildUfwAddArgs(r:RuleInput): string[]`; `function isProtectedPort(port:number): boolean` (22/80/443).

- [ ] **Step 1: `lib/server/firewall/ufw.ts` yaz:**

```ts
export interface UfwRule { num: number; to: string; action: string; from: string; }
export interface UfwStatus { active: boolean; rules: UfwRule[]; }
export interface RuleInput { action: 'allow' | 'deny'; port: number; proto: 'tcp' | 'udp' | 'any'; from?: string; }

const PROTECTED = new Set([22, 80, 443]);
export function isProtectedPort(port: number): boolean { return PROTECTED.has(port); }

/** `ufw status numbered` çıktısını ayrıştırır. */
export function parseUfwStatus(output: string): UfwStatus {
  const active = /Status:\s*active/i.test(output);
  const rules: UfwRule[] = [];
  for (const line of output.split('\n')) {
    // örn: "[ 1] 22/tcp                     ALLOW IN    Anywhere"
    const m = line.match(/^\[\s*(\d+)\]\s+(.+?)\s{2,}(ALLOW|DENY|REJECT|LIMIT)\s+(?:IN|OUT)?\s*(.*)$/i);
    if (m) rules.push({ num: parseInt(m[1], 10), to: m[2].trim(), action: m[3].toUpperCase(), from: (m[4] || '').trim() || 'Anywhere' });
  }
  return { active, rules };
}

const IPV4 = /^(\d{1,3}\.){3}\d{1,3}(\/\d{1,2})?$/;
const IPV6 = /^[0-9a-fA-F:]+(\/\d{1,3})?$/;

/** Kural girişini doğrular; geçerliyse null, değilse hata mesajı. */
export function validateRule(r: RuleInput): string | null {
  if (r.action !== 'allow' && r.action !== 'deny') return 'Geçersiz eylem';
  if (!Number.isInteger(r.port) || r.port < 1 || r.port > 65535) return 'Port 1-65535 olmalı';
  if (!['tcp', 'udp', 'any'].includes(r.proto)) return 'Geçersiz protokol';
  if (r.from && r.from !== 'any' && !IPV4.test(r.from) && !IPV6.test(r.from)) return 'Geçersiz IP';
  return null;
}

/** Doğrulanmış kuraldan ufw argüman dizisi (shell yok). */
export function buildUfwAddArgs(r: RuleInput): string[] {
  const portSpec = r.proto === 'any' ? String(r.port) : `${r.port}/${r.proto}`;
  if (r.from && r.from !== 'any') {
    // ufw allow from <ip> to any port <port> proto <proto>
    const args = [r.action, 'from', r.from, 'to', 'any', 'port', String(r.port)];
    if (r.proto !== 'any') args.push('proto', r.proto);
    return args;
  }
  return [r.action, portSpec];
}
```

- [ ] **Step 2: Failing test** — `lib/server/firewall/ufw.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert';
import { parseUfwStatus, validateRule, buildUfwAddArgs, isProtectedPort } from './ufw';

test('parseUfwStatus: active + numbered rules', () => {
  const out = [
    'Status: active', '',
    '     To                         Action      From',
    '     --                         ------      ----',
    '[ 1] 22/tcp                     ALLOW IN    Anywhere',
    '[ 2] 443/tcp                    ALLOW IN    Anywhere',
    '[ 3] 8080                       DENY IN     192.168.1.5',
  ].join('\n');
  const s = parseUfwStatus(out);
  assert.strictEqual(s.active, true);
  assert.strictEqual(s.rules.length, 3);
  assert.deepStrictEqual({ n: s.rules[0].num, to: s.rules[0].to, a: s.rules[0].action }, { n: 1, to: '22/tcp', a: 'ALLOW' });
  assert.strictEqual(s.rules[2].from, '192.168.1.5');
});

test('parseUfwStatus: inactive', () => {
  assert.deepStrictEqual(parseUfwStatus('Status: inactive'), { active: false, rules: [] });
});

test('validateRule', () => {
  assert.strictEqual(validateRule({ action: 'allow', port: 22, proto: 'tcp' }), null);
  assert.strictEqual(validateRule({ action: 'allow', port: 0, proto: 'tcp' }), 'Port 1-65535 olmalı');
  assert.strictEqual(validateRule({ action: 'allow', port: 22, proto: 'tcp', from: 'bad ip' }), 'Geçersiz IP');
  assert.strictEqual(validateRule({ action: 'allow', port: 22, proto: 'tcp', from: '10.0.0.1' }), null);
  // @ts-expect-error
  assert.strictEqual(validateRule({ action: 'drop', port: 22, proto: 'tcp' }), 'Geçersiz eylem');
});

test('buildUfwAddArgs', () => {
  assert.deepStrictEqual(buildUfwAddArgs({ action: 'allow', port: 443, proto: 'tcp' }), ['allow', '443/tcp']);
  assert.deepStrictEqual(buildUfwAddArgs({ action: 'allow', port: 53, proto: 'any' }), ['allow', '53']);
  assert.deepStrictEqual(buildUfwAddArgs({ action: 'deny', port: 8080, proto: 'tcp', from: '10.0.0.5' }),
    ['deny', 'from', '10.0.0.5', 'to', 'any', 'port', '8080', 'proto', 'tcp']);
});

test('isProtectedPort', () => {
  assert.strictEqual(isProtectedPort(22), true);
  assert.strictEqual(isProtectedPort(443), true);
  assert.strictEqual(isProtectedPort(8080), false);
});
```

- [ ] **Step 3:** `npm test` FAIL→PASS; `npx tsc --noEmit` temiz.
- [ ] **Step 4: Commit** `git add lib/server/firewall/ufw.ts lib/server/firewall/ufw.test.ts && git commit -m "feat(firewall): ufw parser + rule builder/validator + tests"`

---

### Task 2: Backend exec + route'lar + api-client (SSH-safety)

**Files:** Create `lib/server/firewall/exec.ts`, `app/api/firewall/route.ts`, `app/api/firewall/[num]/route.ts`, `app/api/firewall/toggle/route.ts`. Modify `lib/api-client.ts`.

**Interfaces:** Consumes Task 1. Produces: `GET /api/firewall`→`{status:UfwStatus}`; `POST /api/firewall` `{rule:RuleInput}`→add; `DELETE /api/firewall/[num]`→sil (22/tcp allow korumalı); `POST /api/firewall/toggle` `{enable:boolean}`→enable(SSH-safe)/disable. api-client `fwStatus/fwAdd/fwDelete/fwToggle`.

- [ ] **Step 1: `lib/server/firewall/exec.ts`:**
```ts
import { execFile } from 'node:child_process';
import { parseUfwStatus, buildUfwAddArgs, type RuleInput, type UfwStatus } from './ufw';

function run(args: string[]): Promise<string> {
  return new Promise((resolve, reject) => {
    execFile('ufw', args, { timeout: 15000, maxBuffer: 1024 * 1024 }, (e, out, se) =>
      e ? reject(new Error(se || e.message)) : resolve(out));
  });
}
export async function ufwStatus(): Promise<UfwStatus> { return parseUfwStatus(await run(['status', 'numbered'])); }
export async function ufwAdd(r: RuleInput): Promise<void> { await run(buildUfwAddArgs(r)); }
export async function ufwDeleteByNum(n: number): Promise<void> { await run(['--force', 'delete', String(n)]); }
export async function ufwDisable(): Promise<void> { await run(['disable']); }
/** SSH-safe enable: 22/80/443 allow garantile, sonra enable. */
export async function ufwEnable(): Promise<void> {
  for (const spec of ['22/tcp', '80/tcp', '443/tcp']) { try { await run(['allow', spec]); } catch { /* zaten var olabilir */ } }
  await run(['--force', 'enable']);
}
```

- [ ] **Step 2: `app/api/firewall/route.ts`** — GET (requireAuth→`{status: await ufwStatus()}`); POST (requireAuth→`{rule}`→`validateRule`→400 if msg→`ufwAdd`→`{status}`).
- [ ] **Step 3: `app/api/firewall/[num]/route.ts`** — DELETE (requireAuth→num parse→**mevcut status'tan o num'un kuralını bul; eğer `22/tcp` ALLOW ise → 400 'SSH kuralı silinemez (kilitlenme koruması)'**→`ufwDeleteByNum`→`{status}`).
- [ ] **Step 4: `app/api/firewall/toggle/route.ts`** — POST (requireAuth→`{enable:boolean}`→ enable? `ufwEnable()` : `ufwDisable()` →`{status}`).
- [ ] **Step 5: api-client:**
```ts
  fwStatus: () => request('GET', '/firewall'),
  fwAdd: (rule: unknown) => request('POST', '/firewall', { rule }),
  fwDelete: (num: number) => request('DELETE', `/firewall/${num}`),
  fwToggle: (enable: boolean) => request('POST', '/firewall/toggle', { enable }),
```
- [ ] **Step 6:** `npx tsc --noEmit` + `npm run build` + `npm test` PASS.
- [ ] **Step 7: Commit** `git add lib/server/firewall/exec.ts "app/api/firewall" lib/api-client.ts && git commit -m "feat(firewall): ufw routes (status/add/delete/toggle) with SSH-lockout protection"`

---

### Task 3: Frontend firewall sayfası + nav + i18n

**Files:** Create `app/(panel)/firewall/page.tsx`. Modify `app/(panel)/layout.tsx`, `messages/{6}.json`.

- [ ] **Step 1: i18n (6 dil)** — `firewall` bloğu + `nav.firewall`. Anahtarlar: `title, status, active, inactive, enable, disable, rules, addRule, action, allow, deny, port, protocol, from, anywhere, delete, deleteConfirm, empty, sshWarning, enableWarning, num, to`. `nav.firewall`: tr "Güvenlik Duvarı", en "Firewall", zh "防火墙", es "Cortafuegos", de "Firewall", fr "Pare-feu". sshWarning tr: "22 (SSH) kuralı kilitlenmeyi önlemek için silinemez". enableWarning tr: "Etkinleştirmeden önce SSH/HTTP(S) portları otomatik izinli olacak.". Tüm 6 dilde parity.

- [ ] **Step 2: `app/(panel)/firewall/page.tsx` ('use client')** — `api.fwStatus()` → durum rozeti (active yeşil / inactive gri) + enable/disable toggle (disable doğrudan; enable → enableWarning'li onay → `fwToggle(true)`). Kurallar tablosu (num/to/action/from). Her satır Sil (ConfirmDestructive; 22/tcp ALLOW satırında Sil butonu **disabled** + `sshWarning` tooltip). Toolbar "Kural ekle" → modal (action allow/deny, port number, proto tcp/udp/any, from opsiyonel IP) → client `validateRule` (import `@/lib/server/firewall/ufw` — saf, node import yok → güvenli) → `fwAdd`. Her mutasyon sonrası dönen `{status}` ile tabloyu tazele. `useToast`. Mobil: tablo overflow-x, toolbar wrap.

- [ ] **Step 3: nav** — lucide `Shield`; `NAV_ITEMS`'a `{ id:'firewall', icon: Shield, href:'/firewall' }` (cron'dan sonra).
- [ ] **Step 4:** `npx tsc --noEmit` + `npm run build` + `npm test` (i18n parity) PASS.
- [ ] **Step 5: Commit** `git add "app/(panel)/firewall" "app/(panel)/layout.tsx" lib/api-client.ts messages/tr.json messages/en.json messages/zh.json messages/es.json messages/de.json messages/fr.json && git commit -m "feat(firewall): firewall page + nav + i18n"`

---

### Task 4: e2e + deploy (canlı SADECE okuma)

**Files:** Create `e2e/firewall.spec.ts`.

- [ ] **Step 1: e2e** — login → "Güvenlik Duvarı" nav → `/firewall` → h2 görünür → durum rozeti görünür → "Kural ekle" butonu → doğrudan goto SSR çökmez → 360px taşma yok.
- [ ] **Step 2:** `npx tsc --noEmit`; `npm test`; `npm run e2e` (firewall + mevcut PASS; backups stale→`rm -rf .next`+tekrar). `git push` → CI yeşil.
- [ ] **Step 3: Deploy** `bash deploy.sh` → health + caddy Valid.
- [ ] **Step 4: Canlı doğrulama — SADECE OKUMA.** `/firewall` 200; status okunur (ufw inactive görünür). **ufw enable/kural değişikliği YAPMA** (kilitlenme riski; gerçek aktivasyon kullanıcının bilinçli kararı). `/api/firewall` no-auth→401. 22/tcp sil koruması: bir DELETE denemesi (auth'suz→401; mantık unit/koddan doğrulanır).
- [ ] **Step 5: Ledger + tamam.**

## Self-Review (yazar)
- Kapsam: parser/builder/validator→T1; exec+route'lar (SSH-safety)→T2; UI→T3; e2e+deploy→T4. SSH-lockout koruması: enable 22/80/443-allow-önce (exec), 22/tcp-allow silme reddi (route+UI disabled). Güvenlik: requireAuth her route, rule validate, ufw arg dizisi. Tip tutarlılığı: `UfwRule`/`UfwStatus`/`RuleInput`/`parseUfwStatus`/`validateRule`/`buildUfwAddArgs`/`isProtectedPort` T1↔T2↔T3. Canlı doğrulama read-only (production kilit riski). Placeholder yok.
