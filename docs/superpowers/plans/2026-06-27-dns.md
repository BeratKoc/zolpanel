# DNS Management (Cloudflare) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Panelden Cloudflare DNS kayıtlarını yönet (A/AAAA/CNAME/TXT/MX listele/ekle/düzenle/sil). API token panele şifreli kaydedilir.

**Architecture:** Yeniden kullanılabilir **şifreli settings-kv** (db.ts + AES-GCM secrets helper — S3/2FA da kullanacak) + saf DNS kayıt doğrulayıcı çekirdek; Cloudflare API client (fetch); Next route'ları; frontend DNS sayfası.

**Tech Stack:** Next.js 15 route handlers (nodejs), better-sqlite3 (settings tablosu), node:crypto (AES-256-GCM), fetch (Cloudflare API), TypeScript, node:test, next-intl (6 dil), Playwright.

## Global Constraints
- CF API token **şifreli saklanır** (AES-256-GCM, anahtar JWT_SECRET'tan türetilir). Token API yanıtlarında asla düz dönmez (yalnız "ayarlı mı" bilgisi).
- DNS kaydı doğrulanır: type ∈ {A,AAAA,CNAME,TXT,MX}, name non-empty, content non-empty, ttl integer (1=auto veya 60-86400), MX → priority gerekli (integer).
- Hepsi `requireAuth`. Cloudflare çağrıları sunucudan (token istemciye gitmez). Commit yazarı `BeratKoc <ahmetberatkoc0@gmail.com>`; **Co-Authored-By YOK**.
- Test `npm test`; build `npm run build` (Win EPERM→`rm -rf .next`); `npx tsc --noEmit` temiz; 6-dil i18n parity.
- Mevcut: `requireAuth`/`unauthorized`; `api` `request`; nav + lucide; `lib/server/db.ts` (better-sqlite3, `initDb` şema).

## Dosya yapısı
- `lib/server/secrets.ts` — `encryptSecret`/`decryptSecret` (AES-GCM). [YENİ — S3/2FA tarafından da kullanılacak]
- `lib/server/db.ts` — `settings` tablosu + `getSetting`/`setSetting`/`deleteSetting`.
- `lib/server/dns/validate.ts` — `validateDnsRecord` + tipler.
- `lib/server/secrets.test.ts`, `lib/server/dns/validate.test.ts` — unit testler.
- `lib/server/dns/cloudflare.ts` — CF client (zones/records CRUD).
- `app/api/dns/token/route.ts` (GET status, POST save, DELETE), `app/api/dns/zones/route.ts` (GET), `app/api/dns/records/route.ts` (GET ?zoneId, POST), `app/api/dns/records/[id]/route.ts` (PUT, DELETE).
- `app/(panel)/dns/page.tsx`; nav; `lib/api-client.ts`; `messages/{6}.json`.

---

### Task 1: Şifreli settings-kv + secrets helper + DNS doğrulayıcı + testler

**Files:** Create `lib/server/secrets.ts`, `lib/server/secrets.test.ts`, `lib/server/dns/validate.ts`, `lib/server/dns/validate.test.ts`. Modify `lib/server/db.ts`.

**Interfaces:** Produces `encryptSecret(plain:string):string`, `decryptSecret(blob:string):string` (`lib/server/secrets.ts`); `getSetting(key):string|null`, `setSetting(key,value):void`, `deleteSetting(key):void` (`db.ts`); `interface DnsRecordInput { type:string; name:string; content:string; ttl:number; priority?:number }`, `validateDnsRecord(r):string|null` (`dns/validate.ts`).

- [ ] **Step 1: `lib/server/secrets.ts`:**
```ts
import crypto from 'node:crypto';

const KEY = crypto.createHash('sha256').update(process.env.JWT_SECRET || 'zolpanel-dev-fallback').digest();

/** AES-256-GCM ile şifreler → "iv:tag:cipher" (base64). */
export function encryptSecret(plain: string): string {
  const iv = crypto.randomBytes(12);
  const cipher = crypto.createCipheriv('aes-256-gcm', KEY, iv);
  const enc = Buffer.concat([cipher.update(plain, 'utf8'), cipher.final()]);
  const tag = cipher.getAuthTag();
  return [iv.toString('base64'), tag.toString('base64'), enc.toString('base64')].join(':');
}

/** "iv:tag:cipher" çözer; bozuk/oynanmışsa throw. */
export function decryptSecret(blob: string): string {
  const [ivB, tagB, encB] = blob.split(':');
  if (!ivB || !tagB || !encB) throw new Error('Bozuk şifreli veri');
  const decipher = crypto.createDecipheriv('aes-256-gcm', KEY, Buffer.from(ivB, 'base64'));
  decipher.setAuthTag(Buffer.from(tagB, 'base64'));
  return Buffer.concat([decipher.update(Buffer.from(encB, 'base64')), decipher.final()]).toString('utf8');
}
```

- [ ] **Step 2: db.ts settings tablosu + fonksiyonlar.** `initDb`'deki şema bloğuna ekle: `CREATE TABLE IF NOT EXISTS settings (key TEXT PRIMARY KEY, value TEXT NOT NULL);`. Sonra db.ts'e export'lar ekle (mevcut better-sqlite3 `db` handle desenini izle):
```ts
export function getSetting(key: string): string | null {
  const row = db().prepare('SELECT value FROM settings WHERE key=?').get(key) as { value: string } | undefined;
  return row ? row.value : null;
}
export function setSetting(key: string, value: string): void {
  db().prepare('INSERT INTO settings (key,value) VALUES (?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value').run(key, value);
}
export function deleteSetting(key: string): void {
  db().prepare('DELETE FROM settings WHERE key=?').run(key);
}
```
(NOT: `db()` accessor adını mevcut db.ts'teki gerçek isimle eşle — dosyayı oku, getUserByName vb. nasıl erişiyorsa aynısını kullan.)

- [ ] **Step 3: `lib/server/dns/validate.ts`:**
```ts
export interface DnsRecordInput { type: string; name: string; content: string; ttl: number; priority?: number; }
const TYPES = new Set(['A', 'AAAA', 'CNAME', 'TXT', 'MX']);
export function validateDnsRecord(r: DnsRecordInput): string | null {
  if (!TYPES.has(r.type)) return 'Geçersiz kayıt tipi';
  if (typeof r.name !== 'string' || !r.name.trim()) return 'Ad boş olamaz';
  if (typeof r.content !== 'string' || !r.content.trim()) return 'İçerik boş olamaz';
  if (!Number.isInteger(r.ttl) || (r.ttl !== 1 && (r.ttl < 60 || r.ttl > 86400))) return 'TTL 1 (auto) veya 60-86400 olmalı';
  if (r.type === 'MX' && (!Number.isInteger(r.priority) || (r.priority as number) < 0)) return 'MX kaydı için öncelik gerekli';
  return null;
}
```

- [ ] **Step 4: testler** — `lib/server/secrets.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert';
import { encryptSecret, decryptSecret } from './secrets';
test('encrypt→decrypt round-trip', () => {
  const s = 'cf-token-abc123!@#';
  const blob = encryptSecret(s);
  assert.notStrictEqual(blob, s);
  assert.strictEqual(decryptSecret(blob), s);
});
test('tampered blob throws', () => {
  const blob = encryptSecret('x');
  const parts = blob.split(':'); parts[2] = Buffer.from('tampered').toString('base64');
  assert.throws(() => decryptSecret(parts.join(':')));
});
test('malformed throws', () => { assert.throws(() => decryptSecret('garbage')); });
```
`lib/server/dns/validate.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert';
import { validateDnsRecord } from './validate';
test('valid records', () => {
  assert.strictEqual(validateDnsRecord({ type: 'A', name: 'app', content: '1.2.3.4', ttl: 1 }), null);
  assert.strictEqual(validateDnsRecord({ type: 'MX', name: '@', content: 'mail.x.com', ttl: 3600, priority: 10 }), null);
});
test('invalid', () => {
  assert.strictEqual(validateDnsRecord({ type: 'ZZ', name: 'a', content: 'b', ttl: 1 }), 'Geçersiz kayıt tipi');
  assert.strictEqual(validateDnsRecord({ type: 'A', name: '', content: 'b', ttl: 1 }), 'Ad boş olamaz');
  assert.strictEqual(validateDnsRecord({ type: 'A', name: 'a', content: 'b', ttl: 5 }), 'TTL 1 (auto) veya 60-86400 olmalı');
  assert.strictEqual(validateDnsRecord({ type: 'MX', name: 'a', content: 'b', ttl: 1 }), 'MX kaydı için öncelik gerekli');
});
```

- [ ] **Step 5:** `npm test` FAIL→PASS; `npx tsc --noEmit` temiz; `npm run build` PASS (db şeması derlenir).
- [ ] **Step 6: Commit** `git add lib/server/secrets.ts lib/server/secrets.test.ts lib/server/db.ts lib/server/dns/validate.ts lib/server/dns/validate.test.ts && git commit -m "feat(dns): encrypted settings kv + secrets helper + dns record validator"`

---

### Task 2: Cloudflare client + route'lar + api-client

**Files:** Create `lib/server/dns/cloudflare.ts`, `app/api/dns/token/route.ts`, `app/api/dns/zones/route.ts`, `app/api/dns/records/route.ts`, `app/api/dns/records/[id]/route.ts`. Modify `lib/api-client.ts`.

**Interfaces:** Consumes Task 1 (`getSetting`/`setSetting`/`deleteSetting`, `encryptSecret`/`decryptSecret`, `validateDnsRecord`); `requireAuth`. Token settings key = `'cf_api_token'`. Produces: token GET(status)/POST(save)/DELETE; zones GET; records GET(?zoneId)/POST; records [id] PUT/DELETE. api-client `dnsTokenStatus/dnsTokenSave/dnsTokenDelete/dnsZones/dnsRecords/dnsRecordCreate/dnsRecordUpdate/dnsRecordDelete`.

- [ ] **Step 1: `lib/server/dns/cloudflare.ts`:**
```ts
import { getSetting } from '../db';
import { decryptSecret } from '../secrets';

const CF = 'https://api.cloudflare.com/client/v4';

function token(): string {
  const blob = getSetting('cf_api_token');
  if (!blob) throw new Error('Cloudflare API token ayarlı değil');
  return decryptSecret(blob);
}
async function cf(path: string, init?: RequestInit): Promise<any> {
  const res = await fetch(`${CF}${path}`, {
    ...init,
    headers: { Authorization: `Bearer ${token()}`, 'Content-Type': 'application/json', ...(init?.headers || {}) },
  });
  const data = await res.json();
  if (!res.ok || data.success === false) {
    throw new Error(data.errors?.[0]?.message || `Cloudflare hatası (${res.status})`);
  }
  return data.result;
}
export const cloudflare = {
  listZones: () => cf('/zones?per_page=50'),
  listRecords: (zoneId: string) => cf(`/zones/${zoneId}/dns_records?per_page=200`),
  createRecord: (zoneId: string, body: unknown) => cf(`/zones/${zoneId}/dns_records`, { method: 'POST', body: JSON.stringify(body) }),
  updateRecord: (zoneId: string, recId: string, body: unknown) => cf(`/zones/${zoneId}/dns_records/${recId}`, { method: 'PUT', body: JSON.stringify(body) }),
  deleteRecord: (zoneId: string, recId: string) => cf(`/zones/${zoneId}/dns_records/${recId}`, { method: 'DELETE' }),
};
```

- [ ] **Step 2: token route** — `app/api/dns/token/route.ts`: GET (requireAuth → `{ configured: !!getSetting('cf_api_token') }` — token DÖNDÜRME); POST `{token}` (requireAuth → non-empty → `setSetting('cf_api_token', encryptSecret(token))` → `{ok:true}`); DELETE (requireAuth → `deleteSetting('cf_api_token')` → `{ok:true}`).

- [ ] **Step 3: zones + records routes** — `zones/route.ts` GET → `{zones: await cloudflare.listZones()}`; `records/route.ts` GET ?zoneId → `{records: await cloudflare.listRecords(zoneId)}`, POST `{zoneId, record}` → `validateDnsRecord` (400) → `cloudflare.createRecord(zoneId, mapToCf(record))`; `records/[id]/route.ts` PUT `{zoneId, record}` → validate → `updateRecord`, DELETE `{zoneId}` → `deleteRecord`. `mapToCf`: `{type,name,content,ttl,priority?}` → CF body (CF uses same field names + `priority` for MX). Token yoksa cloudflare çağrısı throw → 400/500 + mesaj. requireAuth hepsinde.

- [ ] **Step 4: api-client** (`lib/api-client.ts`):
```ts
  dnsTokenStatus: () => request('GET', '/dns/token'),
  dnsTokenSave: (token: string) => request('POST', '/dns/token', { token }),
  dnsTokenDelete: () => request('DELETE', '/dns/token'),
  dnsZones: () => request('GET', '/dns/zones'),
  dnsRecords: (zoneId: string) => request('GET', `/dns/records?zoneId=${encodeURIComponent(zoneId)}`),
  dnsRecordCreate: (zoneId: string, record: unknown) => request('POST', '/dns/records', { zoneId, record }),
  dnsRecordUpdate: (zoneId: string, id: string, record: unknown) => request('PUT', `/dns/records/${encodeURIComponent(id)}`, { zoneId, record }),
  dnsRecordDelete: (zoneId: string, id: string) => request('DELETE', `/dns/records/${encodeURIComponent(id)}`, { zoneId }),
```

- [ ] **Step 5:** `npx tsc --noEmit` + `npm run build` + `npm test` PASS.
- [ ] **Step 6: Commit** `git add lib/server/dns/cloudflare.ts "app/api/dns" lib/api-client.ts && git commit -m "feat(dns): cloudflare client + token/zones/records routes + api-client"`

---

### Task 3: Frontend DNS sayfası + nav + i18n

**Files:** Create `app/(panel)/dns/page.tsx`. Modify `app/(panel)/layout.tsx`, `messages/{6}.json`.

- [ ] **Step 1: i18n (6 dil)** — `dns` bloğu + `nav.dns`. Anahtarlar: `title, tokenNeeded, tokenSave, tokenSaved, tokenDelete, tokenPlaceholder, zone, selectZone, records, addRecord, type, name, content, ttl, priority, auto, edit, delete, save, cancel, deleteConfirm, empty, noToken`. `nav.dns`: tr "DNS", en "DNS", zh "DNS", es "DNS", de "DNS", fr "DNS". noToken tr: "Cloudflare API token gerekli — ayarlayın". Tüm 6 dilde parity.

- [ ] **Step 2: `app/(panel)/dns/page.tsx` ('use client')** — Önce `api.dnsTokenStatus()`. `configured=false` ise: token giriş kartı (`<input type=password>` + Kaydet → `dnsTokenSave`). `configured=true` ise: zone seçici (`dnsZones()` → dropdown), seçili zone → `dnsRecords(zoneId)` tablosu (type/name/content/ttl/priority). "Kayıt ekle" → modal (type select, name, content, ttl (auto=1), MX ise priority) → client `validateDnsRecord` (import `@/lib/server/dns/validate` — saf) → `dnsRecordCreate`. Satır: Düzenle (modal) / Sil (ConfirmDestructive → `dnsRecordDelete`). Token-değiştir butonu (delete → giriş kartına dön). `useToast`. Mobil: tablo overflow-x, toolbar wrap.

- [ ] **Step 3: nav** — lucide `Globe2` (veya `Network`); `NAV_ITEMS`'a `{ id:'dns', icon: Globe2, href:'/dns' }` (firewall'dan sonra).
- [ ] **Step 4:** `npx tsc --noEmit` + `npm run build` + `npm test` (i18n parity) PASS.
- [ ] **Step 5: Commit** `git add "app/(panel)/dns" "app/(panel)/layout.tsx" lib/api-client.ts messages/tr.json messages/en.json messages/zh.json messages/es.json messages/de.json messages/fr.json && git commit -m "feat(dns): dns management page + nav + i18n"`

---

### Task 4: e2e + deploy

**Files:** Create `e2e/dns.spec.ts`.

- [ ] **Step 1: e2e** — login → "DNS" nav → `/dns` → h2 görünür → token ayarlı değilse token giriş kartı görünür (CI'da token yok → bu dal) → doğrudan goto SSR çökmez → 360px taşma yok.
- [ ] **Step 2:** `npx tsc --noEmit`; `npm test`; `npm run e2e` (dns + mevcut PASS; backups stale→`rm -rf .next`+tekrar). `git push` → CI yeşil.
- [ ] **Step 3: Deploy** `bash deploy.sh` → health + caddy Valid.
- [ ] **Step 4: Canlı doğrulama** — `/dns` 200; token ayarlı değilse "token gerekli" kartı; `/api/dns/token` no-auth→401. (Token'lı uçtan-uca doğrulama kullanıcının CF token'ı girmesiyle.)
- [ ] **Step 5: Ledger + tamam.**

## Self-Review (yazar)
- Kapsam: secrets+settings-kv+validator→T1; CF client+route'lar→T2; UI→T3; e2e+deploy→T4. Güvenlik: token AES-GCM şifreli, asla düz dönmez, requireAuth her route, CF çağrısı sunucudan, kayıt validate. Tip tutarlılığı: `encryptSecret`/`decryptSecret`/`getSetting`/`setSetting`/`validateDnsRecord`/`DnsRecordInput` T1↔T2↔T3. settings-kv + secrets S3/2FA'da yeniden kullanılacak. Placeholder yok. NOT: db.ts `db()` accessor adı dosyadan doğrulanmalı (implementer).
